# Where the frontend reaches the FastAPI backend (backend/main.py).
VITE_API_BASE_URL=http://localhost:8000

# Optional. GA4 measurement ID (e.g. G-XXXXXXXXXX). Analytics stays fully
# disabled — no script loads, no events fire — until this is set.
VITE_GA_MEASUREMENT_ID=
